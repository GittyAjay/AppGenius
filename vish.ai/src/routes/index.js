// routes.js
const {
    submitRoute,
    downloadRoute
} = require('./form-route');

const {
    routeRoute,
    serverRoute
} = require('./route-route');

module.exports = {
    submitRoute,
    downloadRoute,
    routeRoute,
    serverRoute
};
